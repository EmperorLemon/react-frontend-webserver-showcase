import { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <div>
            <h1 style={{display: "flex", justifyContent: "center"}}>Welcome Home</h1>
        </div>
    );
};

export default Home;